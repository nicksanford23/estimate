# Codex Probe Scripts

Put Codex-created exploratory scripts here during the `impl-plan-codex` branch.
Do not place new probe scripts in the repo-level `scripts/` directory unless
the user explicitly asks to promote them into the main project.
