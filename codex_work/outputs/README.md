# Codex Probe Outputs

Generated probe outputs from Codex-owned scripts go here, for example JSONL,
CSV, and Markdown summaries. Keep these separate from the main `data/` outputs
until the user decides the probe should become part of the project.
